import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FermaGen AI - Precision Fermentation Platform",
  description: "AI-powered precision fermentation platform for faster, cheaper, and sustainable alternative protein R&D",
  keywords: ["precision fermentation", "AI", "protein", "sustainability", "biotech"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
